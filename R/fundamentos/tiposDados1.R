# Tipos de dados: caractere, numérico, inteiro e fator

# Atribuição de valor: = ou <-
delta <- 8
delta = 8

# Declaração de variáveis implícita

delta <- 8 #ponto flutuante
delta <- 8L #inteiro
logico <- TRUE
logico <- F
caractere <- "Texto"

# Operadores: +, -, *, /, ^, %%, %/%
# Operadores lógicos: <, >, <=, >=, ==, !=, !, |, &
# Funções matemáticas: abs, sqrt, sum, log, cos, sin, tan, exp